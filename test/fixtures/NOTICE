test/fixtures 说明
==================

voice-zh-16k.wav
----------------
来源：维护者手机自录音频（WAV，48kHz 立体声 24bit，7.005s / 1,970KB），非第三方素材、无版权问题。
用途：真模型守门回归（scripts/smoke.mjs 的慢速 lane）。用于锁住「同一素材的不同长度切片
      判定出的累计人声时长必须不同且均高于门限」这一语义——0.3.0 的 P0（整段一次性喂入
      Silero VAD 导致判定恒为 314ms）正是靠这条断言被抓住。

入库形态：源素材全长转码为 16kHz 单声道 PCM16（112,085 样本 / 7.005s / 219KB，不裁剪）。
          这正是生产链路真实的输入格式（浏览器 AudioContext({sampleRate:16000}) + encodeWav）。

转码配方（一次性开发步骤，源 WAV 与转码脚本不入库）：
  1. 解析源 WAV 的 fmt/data chunk，按 24bit 小端读取样本；
  2. 立体声下混单声道（左右声道算术平均）；
  3. 复用 lib/pure.js 的 linearResample(mono, 48000, 16000) 重采样到 16k；
  4. 复用 lib/pure.js 的 encodeWav(samples, 16000) 写出 PCM16 WAV。
  —— 即「用插件自己的数学生成 fixture」，保证产物与线上链路的解码/重采样语义一致。

分发：test/ 不在 package.json 的 files 列表内，本 fixture 只进 GitHub 仓库，不随 npm 包分发。
